package tree;

public class TreeTest {

    public static void main(String[] args) {

        // 리스트 - 순서대로 데이터를 나열하는 자료구조
        // 트리 - 데이터 사이의 계층 구조를 표현
        // 트리 구성 요소 - 노드와 가지

        // 루트 - 트리의 가장 윗 부분에 속하는 부분
        // 리프 leaf -  (terminal node, external node) - 트리의 가장 아랫 부분에 위치하는 노드 (더 이상 뻗어 나갈 곳이 없는 노드)
        // 안쪽 노드 (non-terminal-node) - 루트를 포함하여
        // 자식 child - 어떤 노트도부터 가지로 연결 된 아래쪽 노드
        // 부모 parents - 어떤 노드에서 가지로 연결 된 위쪽 노드
        // 차수 degree - 노드가 갖는 자식의 수

        // 이진 트리 - 노드가 왼쪽 자식과 오른쪽 자식을 갖는 트리를 이진 트리 binary tree 라고 함
        // 이때 각 노드 자식은 2명 이하만 유지
        // binary search tree
        // 1. 어떤 노드 N 을 기준으로 왼쪽 서브 트리 노드의 모든 키 값은 노드 N 의 기 캆보다 작아야 합니다.
        // 2. 오른쪽 서브 트리 노드의 키 값은 노드 N 의 키 값보다 커야 합니다.
        // 3. 같은 키 값을 갖는 노드는 없습니다.

    }
}

